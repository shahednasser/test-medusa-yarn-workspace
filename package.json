{
  "name": "test-storefront-hosting",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/backend",
    "apps/storefront"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace backend dev\" \"yarn workspace storefront dev\"",
    "dev:backend": "yarn workspace backend dev",
    "dev:storefront": "yarn workspace storefront dev",
    "build": "yarn workspace backend build && yarn workspace storefront build",
    "build:backend": "yarn workspace backend build",
    "build:storefront": "yarn workspace storefront build",
    "start": "concurrently \"yarn workspace backend start\" \"yarn workspace storefront start\"",
    "start:backend": "yarn workspace backend start",
    "start:storefront": "yarn workspace storefront start"
  },
  "packageManager": "yarn@3.6.4",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
